/**
 * 框架集成配置类。
 * <ul>
 *   <li>MyBatisPlusConfig — 分页插件 + 乐观锁插件</li>
 *   <li>RedisConfig — RedisTemplate 序列化配置（JSON 格式存储）</li>
 *   <li>WebMvcConfig — CORS 跨域配置（前后端分离）</li>
 *   <li>SwaggerConfig — SpringDoc OpenAPI 文档配置</li>
 * </ul>
 */
package com.seeleaf.hotel.config;
